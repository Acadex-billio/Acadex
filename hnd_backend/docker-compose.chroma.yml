services:
  chroma:
    image: chromadb/chroma:latest
    container_name: hnd-chroma
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      IS_PERSISTENT: "TRUE"
      ALLOW_RESET: "TRUE"
    volumes:
      - ./chroma_data:/chroma/chroma
